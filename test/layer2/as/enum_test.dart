import 'package:test/test.dart';

void main() {
    group("", () {

    });
//    group("$ListToggle", () {
//        final List<int> list = [1, 2, 3];
//
//        test("direction can't be null", () {
//            expect(() => ListToggle.walk(list, 1, direction: null), throwsA(const TypeMatcher<AssertionError>()));
//        });
//
//        test("list can't be null", () {
//            expect(() => ListToggle.walk(null, 1), throwsA(const TypeMatcher<AssertionError>()));
//        });
//
//        test("returns first item in the list if current item is not in it", () {
//            expect(ListToggle.walk(list, 10), 1);
//        });
//
//        test("toggle forwards", () {
//            expect(ListToggle.walk(list, 1, direction: ToggleDirection.forwards), 2);
//            expect(ListToggle.walk(list, 2, direction: ToggleDirection.forwards), 3);
//            expect(ListToggle.walk(list, 3, direction: ToggleDirection.forwards), 1);
//        });
//
//        test("toggle backwards", () {
//            expect(ListToggle.walk(list, 1, direction: ToggleDirection.backwards), 3);
//            expect(ListToggle.walk(list, 2, direction: ToggleDirection.backwards), 1);
//            expect(ListToggle.walk(list, 3, direction: ToggleDirection.backwards), 2);
//        });
//    });
}
